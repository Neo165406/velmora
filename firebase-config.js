// Fill these in from: Firebase Console → Project Settings → General → "Your apps" → SDK setup and configuration.
// This is the PUBLIC web app config (apiKey here is a client identifier, not a secret) — safe to commit.
// Do NOT put a service-account / private key here — that's a different, sensitive credential.

export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
