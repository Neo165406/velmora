// Get a free API key at https://api.imgbb.com/ (sign in, then "Add API key").
// Paste it below — this is the same key you used for StrDust's dashboard,
// or a new one if you'd rather keep the two projects separate.

export const imgbbApiKey = "";
